<h1>Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <div>
      <%= link_to post.title, post %>
      <% if user_signed_in? %>
        <p> by: <%= post.user.username %></p>
      <% end %>
      </div>
    </li>
  <% end %>
</ul>

<%= link_to "New Post", new_post_path %>